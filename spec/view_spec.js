define(function(require) {
    var Minionette = require('minionette');
    describe('Minionette.View', function() {
        it('Exists', function() {
            expect(Minionette.View).to.not.be.undefined;
        });
    });
});
